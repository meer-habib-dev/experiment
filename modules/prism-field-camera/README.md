# Prism Field Camera module

Local Apple Expo module for the [Prism Field](../../src/features/prism-field/README.md) experiment.
It owns the AVFoundation capture session, Camera Control events, and composited native view.

This module is not published independently. It is autolinked from `modules/` and requires a new iOS
development build after Swift, podspec, or module configuration changes.
